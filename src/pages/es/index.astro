---
/**
 * Landing Page - Spanish
 */
export const prerender = true;
import BaseLayout from '@/layouts/BaseLayout.astro';
import HeroSection from '@/components/home/HeroSection.astro';
import AboutPreview from '@/components/home/AboutPreview.astro';
import FeaturedConcerts from '@/components/home/FeaturedConcerts.astro';
import ServicesHighlight from '@/components/home/ServicesHighlight.astro';
import { getContent } from '@/utils/i18n';

const lang = 'es';
const content = await getContent(lang, 'home');
---

<BaseLayout
  title={content.meta.title}
  description={content.meta.description}
  lang={lang}
>
  <!-- Hero Section -->
  <HeroSection lang={lang} content={content.hero} />

  <!-- About Preview -->
  <AboutPreview lang={lang} content={content.about} />

  <!-- Featured Concerts -->
  <FeaturedConcerts lang={lang} content={content.concerts} />

  <!-- Services Highlight -->
  <ServicesHighlight lang={lang} content={content.services} />

  <!-- Final CTA con glassmorphism y animaciones -->
  <section class="section-tight animate-on-scroll delay-600">
    <div class="container max-w-4xl">
      <div class="card-glass p-12 rounded-xl text-center">
        <h2 class="text-3xl md:text-4xl font-serif font-bold mb-4">
          {content.cta.heading}
        </h2>
        <p class="text-lg mb-8 text-text-secondary">
          {content.cta.subheading}
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/es/contact"
            class="btn btn-primary px-8 py-4 text-lg"
          >
            {content.cta.buttons.primary}
          </a>
          <a
            href="/es/concerts"
            class="btn btn-secondary px-8 py-4 text-lg"
          >
            {content.cta.buttons.secondary}
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>


<style>
  /* Stagger animation delays */
  .stagger-1 { animation-delay: 100ms; }
  .stagger-2 { animation-delay: 200ms; }
  .stagger-3 { animation-delay: 300ms; }
</style>
