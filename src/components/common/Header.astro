---
/**
 * Header Component - Main navigation with logo, menu, theme toggle, and language switch
 */
import ThemeToggle from '@/components/common/ThemeToggle.astro';
import LanguageSwitch from '@/components/common/LanguageSwitch.astro';

export interface Props {
  lang: 'en' | 'es';
  currentPath?: string;
}

const { lang, currentPath: providedPath } = Astro.props;
const currentPath = providedPath || Astro.url.pathname;

// Navigation items
const navItems = {
  en: [
    { label: 'Home', href: '/en/' },
    { label: 'About', href: '/en/about' },
    { label: 'Gallery', href: '/en/gallery' },
    { label: 'Concerts', href: '/en/concerts' },
    { label: 'Services', href: '/en/services' },
    { label: 'Contact', href: '/en/contact' },
  ],
  es: [
    { label: 'Inicio', href: '/es/' },
    { label: 'Nosotros', href: '/es/about' },
    { label: 'Galería', href: '/es/gallery' },
    { label: 'Conciertos', href: '/es/concerts' },
    { label: 'Servicios', href: '/es/services' },
    { label: 'Contacto', href: '/es/contact' },
  ],
};

const items = navItems[lang];

const isActive = (href: string) => {
  if (!currentPath) return false;
  if (href === `/${lang}/` || href === `/${lang}`) {
    return currentPath === `/${lang}/` || currentPath === `/${lang}`;
  }
  return currentPath.startsWith(href);
};
---

<header class="header sticky top-0 z-50 bg-bg-primary/95 backdrop-blur-md border-b border-divider shadow-sm">
  <div class="container">
    <nav class="flex items-center justify-between py-4" aria-label={lang === 'en' ? 'Main navigation' : 'Navegación principal'}>
      <!-- Logo -->
      <a href={`/${lang}/`} class="logo flex items-center gap-3 group" aria-label={lang === 'en' ? 'Home' : 'Inicio'}>
        <img
          src="/logos/logo-light.png"
          alt="Cuarteto Metanoia"
          width="180"
          height="60"
          class="h-12 w-auto dark:hidden block transition-transform group-hover:scale-105"
          loading="eager"
        />
        <img
          src="/logos/logo-dark.png"
          alt="Cuarteto Metanoia"
          width="180"
          height="60"
          class="h-12 w-auto hidden dark:block transition-transform group-hover:scale-105"
          loading="eager"
        />
      </a>

      <!-- Desktop Navigation -->
      <ul class="hidden lg:flex items-center gap-8">
        {items.map((item) => (
          <li>
            <a
              href={item.href}
              class={`nav-link ${isActive(item.href) ? 'active' : ''}`}
              aria-current={isActive(item.href) ? 'page' : undefined}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>

      <!-- Actions -->
      <div class="flex items-center gap-4">
        <ThemeToggle lang={lang} />
        <LanguageSwitch lang={lang} currentPath={currentPath} />

        <!-- Mobile Menu Button -->
        <button
          id="mobile-menu-button"
          class="lg:hidden p-2 rounded-lg hover:bg-bg-secondary transition-colors"
          aria-label={lang === 'en' ? 'Toggle menu' : 'Abrir menú'}
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <svg
            class="w-6 h-6"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              class="menu-icon"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"
            />
            <path
              class="close-icon hidden"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"
            />
          </svg>
        </button>
      </div>
    </nav>

    <!-- Mobile Navigation con transición -->
    <div
      id="mobile-menu"
      class="mobile-menu lg:hidden overflow-hidden transition-all duration-300 ease-in-out"
      style="max-height: 0; opacity: 0;"
      aria-label={lang === 'en' ? 'Mobile navigation' : 'Navegación móvil'}
    >
      <ul class="flex flex-col gap-2 pb-4">
        {items.map((item) => (
          <li>
            <a
              href={item.href}
              class={`mobile-nav-link ${isActive(item.href) ? 'active' : ''}`}
              aria-current={isActive(item.href) ? 'page' : undefined}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</header>

<style>
  /* Desktop Navigation Links */
  .nav-link {
    position: relative;
    font-size: var(--font-size-base);
    font-weight: 500;
    color: var(--text-secondary);
    transition: color var(--transition-base);
    padding: var(--space-2) 0;
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--color-primary);
    transition: width var(--transition-base);
  }

  .nav-link:hover,
  .nav-link:focus {
    color: var(--color-primary);
  }

  .nav-link:hover::after,
  .nav-link:focus::after {
    width: 100%;
  }

  .nav-link.active {
    color: var(--color-primary);
    font-weight: 600;
  }

  .nav-link.active::after {
    width: 100%;
  }

  /* Mobile Navigation Links */
  .mobile-nav-link {
    display: block;
    padding: var(--space-3) var(--space-4);
    border-radius: var(--radius-base);
    font-weight: 500;
    color: var(--text-secondary);
    transition: all var(--transition-base);
  }

  .mobile-nav-link:hover,
  .mobile-nav-link:focus {
    background: rgba(var(--color-primary-rgb), 0.1);
    color: var(--color-primary);
  }

  .mobile-nav-link.active {
    background: rgba(var(--color-primary-rgb), 0.15);
    color: var(--color-primary);
    font-weight: 600;
  }

  /* Transición del menú mobile */
  .mobile-menu {
    transition: max-height 0.3s ease-in-out, opacity 0.3s ease-in-out;
  }

  .mobile-menu.open {
    max-height: 500px !important;
    opacity: 1 !important;
  }
</style>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = menuButton?.querySelector('.menu-icon');
  const closeIcon = menuButton?.querySelector('.close-icon');

  menuButton?.addEventListener('click', () => {
    const isExpanded = menuButton.getAttribute('aria-expanded') === 'true';
    const newState = !isExpanded;

    menuButton.setAttribute('aria-expanded', String(newState));

    if (newState) {
      // Abrir menú
      mobileMenu?.classList.add('open');
      mobileMenu?.style.setProperty('max-height', '500px');
      mobileMenu?.style.setProperty('opacity', '1');
    } else {
      // Cerrar menú
      mobileMenu?.classList.remove('open');
      mobileMenu?.style.setProperty('max-height', '0');
      mobileMenu?.style.setProperty('opacity', '0');
    }

    menuIcon?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
  });

  // Cerrar al hacer click fuera
  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    if (!target.closest('header')) {
      mobileMenu?.classList.remove('open');
      mobileMenu?.style.setProperty('max-height', '0');
      mobileMenu?.style.setProperty('opacity', '0');
      menuButton?.setAttribute('aria-expanded', 'false');
      menuIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    }
  });

  // Cerrar con tecla Escape
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      mobileMenu?.classList.remove('open');
      mobileMenu?.style.setProperty('max-height', '0');
      mobileMenu?.style.setProperty('opacity', '0');
      menuButton?.setAttribute('aria-expanded', 'false');
      menuIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    }
  });
</script>
